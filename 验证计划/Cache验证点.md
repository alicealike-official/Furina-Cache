## 一、功能验证

| 验证点 | 验证策略 | 验证方法 | 测试用例/断言类 | 是否通过 |
|--------|----------|----------|----------|----------|
| **读命中** | 先写入特定地址使其缓存，再读取相同地址 | 定向测试 + scoreboard | cache_read_hit_test |
| **读未命中** | 访问未缓存地址，触发内存读取 | 定向测试 + scoreboard | cache_read_miss_test |
| **写命中** | 写已缓存地址，更新缓存数据并置脏 | 定向测试 + scoreboard | cache_write_hit_test |
| **写未命中** | 写未缓存地址，触发写分配和替换 | 定向测试 + scoreboard | cache_write_miss_test |
| **读写一致性** | 同一地址读写交替进行 | 随机测试 + Scoreboard比对 | cache_write_read_test |

## 二、替换策略验证

| 验证点 | 验证策略 | 验证方法 | 测试用例/断言类 | 是否通过 |
|--------|----------|----------|----------|----------|
| **FIFO替换正确性** | 访问序列控制，验证FIFO指向行被替换 | Scoreboard | |
| **优先替换无效行** | 组内存在无效行时新分配应使用无效行 | Scoreboard | |
| **替换脏行写回** | 被替换行若为脏，触发写回操作 | Scoreboard | |
| **替换干净行直接覆盖** | 被替换行若为干净，直接覆盖不写回 | Scoreboard | |
| **替换地址正确性** | 替换时写回地址由Tag+Index组成 | Scoreboard | |
| **全满时替换** | 所有Way有效时触发替换算法 | directed case | cache_fill_full_test |

## 三、Cache数据维护验证

| 验证点 | 验证策略 | 验证方法 | 测试用例/断言类 | 是否通过 |
|--------|----------|----------|----------|----------|
| **Tag匹配正确性** | 相同Tag不同Index不误命中 | directed test | cache_tag_match_test |
| **Valid位管理** | 复位后全0，Fetch后置1| Assertion |  |
| **Dirty位管理** | 读不置脏，写置脏，写回后清脏 | Assertion |  |
| **同一地址多Way冲突** | 同一地址不会出现在多个Way中 | Assertion |  |

## 四、写回策略验证

| 验证点 | 验证策略 | 验证方法 | 测试用例/断言类 | 是否通过 |
|--------|----------|----------|----------|----------|
| **写回触发条件** | 仅当脏行被替换时写回 | Assertion | |
| **写回数据完整性** | 写回数据为缓存中最新数据 | Scoreboard | |
| **写回地址正确性** | 地址由Tag+Index+偏移组成 | Scoreboard | |
| **多次修改后写回** | 同一地址多次修改后写回最终值 | directed case | cache_fifo_fill_test |
 
## 五、边界条件验证

| 验证点 | 验证策略 | 验证方法 | 测试用例/断言类 | 是否通过 |
|--------|----------|----------|----------|----------|
| **全Cache填满** | 所有Set和Way均有效后再访问 | directed test | cache_fill_full_test |
| **空Cache访问** | 复位后首次访问 | directed case | 合并进read_miss_test | cache_read_miss_test |
| **极端参数** | 最小/最大Set、BlockSize、Way数 | multi regression test | |

## 六、时序验证

| 验证点 | 验证策略 | 验证方法 | 测试用例/断言类 | 是否通过 |
|--------|----------|----------|----------|----------|
| **命中延迟** | 命中时Ready信号在规定周期内拉高 | Assertion | |
| **未命中延迟** | 未命中时等待Mem响应后拉高Ready | Assertion | |
| **间断请求** | cpu发出间断请求，确认请求无效的正确行为 | random case | cache_random_test with config|
| **Mem响应延迟变化** | 不同内存延迟下的正确性 | memory delay injection regression | |
| **复位时序** | 复位期间和复位后立即访问 | directed case |
| **异步复位** | 复位立即清除所有状态 | directed case + Assertion |

## 七、接口协议验证

| 验证点 | 验证策略 | 验证方法 | 测试用例/断言类 | 是否通过 |
|--------|----------|----------|----------|----------|
| **CPU接口协议** | 符合约定的握手时序 | Assertion | |
| **内存接口协议** | Mem_Req/Mem_Resp时序正确 | Assertion | |

## 八、随机验证

| 验证点 | 验证策略 | 验证方法 | 测试用例/断言类 | 是否通过 |
|--------|----------|----------|----------|----------|
| **地址分布覆盖** | 均匀/热点/顺序地址分布 | 约束随机 + 功能覆盖率 | cache_random_test
| **操作类型混合** | 读写操作随机比例 | 约束随机 + Scoreboard | cache_random_test
| **背靠背混合访问** | 随机间隔的连续访问 | 随机测试 + 时序检查 | cache_random_test
| **长时间稳定性** | 长时间随机测试无死锁 | 长时间随机仿真 + 断言 | cache_random_test 

## 九、性能验证

| 验证点 | 验证策略 | 验证方法 | 测试用例/断言类 | 是否通过 |
|--------|----------|----------|----------|----------|
| **命中率统计** | 特定工作负载下的命中率 | 性能计数器 + 统计分析 |

## 十、覆盖率驱动验证

| 验证点 | 验证策略 | 验证方法 | 测试用例/断言类 | 是否通过 |
|--------|----------|----------|----------|----------|
| **代码覆盖率** | 行/条件/分支/状态机覆盖率 | 代码覆盖率工具 |
| **功能覆盖率** | Hit/Miss/替换/写回场景覆盖 | 功能覆盖率模型 |
| **交叉覆盖率** | 地址空间×操作类型×Cache状态 | 交叉覆盖率定义 |
| **替换场景覆盖** | 所有替换策略路径 | 覆盖率驱动随机测试 |
